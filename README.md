# Frequently used Git commands at work

### Cloning, Switching to and Tracking a remote branch (within the same remote repo)
- git fetch > fetches remote branches
- git branch -v -a > lists all branches (local and remote) with descriptions
- git switch add-abc-to-xyz

### Listing recent commits with descriptions
- git cherry -v
